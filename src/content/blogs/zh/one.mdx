---
title: "样式展示：完整的博客文章示例"
slug: one-blog-post
description: "这是一个展示所有自定义样式组件的完整示例文章，包含各种 Markdown 元素的美化效果。"
cover: https://image.xiwu.me/2025/6f1f75dc9c4aecab90c832f72c36935d.jpeg
date: 2025-10-08
draft: false
---

这是一个展示所有自定义样式的完整示例。你可以看到各种 Markdown 元素是如何被美化的。

## 标题样式展示

### 三级标题

这里是三级标题的内容，你可以看到它有悬停时的锚点链接效果。

#### 四级标题

四级标题稍小一些，但依然保持良好的层次感。

##### 五级标题

五级标题适合更细分的内容组织。

###### 六级标题

六级标题是最小的标题级别。

## 文本样式

这是一个普通段落，展示了基本的文本样式。段落之间有适当的间距，行高也经过优化以提供更好的阅读体验。

这里有一些**粗体文本**和*斜体文本*，还有~~删除线文本~~。你还可以使用`内联代码`来突出显示代码片段。

这里有一个==高亮文本==的示例。

## 链接样式

这里有一个[内部链接](#标题样式展示)和一个[外部链接](https://github.com)。外部链接会自动在新标签页中打开。

## 列表样式

### 无序列表

- 第一个列表项
- 第二个列表项
  - 嵌套列表项
  - 另一个嵌套项
- 第三个列表项

### 有序列表

1. 第一步
2. 第二步
   1. 子步骤 A
   2. 子步骤 B
3. 第三步

## 引用块

> 这是一个引用块的示例。它有漂亮的左边框和背景色，在深色模式下也有很好的对比度。
>
> 引用块可以包含多个段落，每个段落都会保持统一的样式。

## 代码块

这里是一个内联代码示例：`console.log('Hello World')`

下面是一个代码块：

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
  return `Welcome to our blog, ${name}`;
}

// 调用函数
const message = greet("读者");
console.log(message);
```

```typescript
interface BlogPost {
  title: string;
  description?: string;
  date: string;
  author: string;
  tags: string[];
}

const post: BlogPost = {
  title: "样式展示",
  description: "这是一个示例",
  date: "2025-10-08",
  author: "Xi Wu",
  tags: ["样式", "示例"],
};
```

## 表格样式

| 功能     | 描述                 | 状态    |
| -------- | -------------------- | ------- |
| 标题样式 | 渐变色标题和锚点链接 | ✅ 完成 |
| 代码高亮 | 语法高亮和主题适配   | ✅ 完成 |
| 表格样式 | 响应式表格和悬停效果 | ✅ 完成 |
| 图片优化 | 圆角阴影和懒加载     | ✅ 完成 |

## 分割线

上面是一个表格，下面会有一个漂亮的渐变分割线：

---

## 图片展示

![示例图片](https://image.xiwu.me/2025/6f1f75dc9c4aecab90c832f72c36935d.jpeg)

图片会自动添加圆角、阴影效果，并且支持懒加载以提升性能。

## 总结

这个示例展示了所有的自定义样式组件：

- ✨ **渐变标题**：美观的渐变色标题
- 🔗 **锚点链接**：悬停显示的标题链接
- 🎨 **主题适配**：完美支持深色和浅色模式
- 📱 **响应式设计**：在各种设备上都有良好表现
- 🚀 **性能优化**：图片懒加载和优化的样式

希望你喜欢这些样式！
